# Dimensionality Reduction

This project covers unsupervised learning techniques used to reduce the number of random variables under consideration by obtaining a set of principal variables.

## Files

* `0-pca.py`: Implements Principal Component Analysis (PCA) to reduce the dimensionality of a dataset while maintaining a specified fraction of the original variance. Uses Singular Value Decomposition (SVD) for numerical stability.
