# Attention

This project covers the implementation of the Attention Mechanism, specifically diving into Sequence-to-Sequence (Seq2Seq) models, Encoder-Decoder architectures, and Transformers for machine translation.

## Tasks
* **0. RNN Encoder**: Defines a class `RNNEncoder` that inherits from `tensorflow.keras.layers.Layer` to encode inputs for machine translation.
